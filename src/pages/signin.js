import React, {useState} from 'react';

import {
    SigninForm,
} from '../components';

const Signin = () => {

    return (
        <div>
           <SigninForm/>
        </div>
    )
}

export default Signin;