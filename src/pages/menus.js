// the menus page

import React, {useState} from 'react';

import {
    PageContainer
} from '../components';

const Menus = () => {

    return (
        <div>
           <PageContainer/>
        </div>
    )
}

export default Menus;